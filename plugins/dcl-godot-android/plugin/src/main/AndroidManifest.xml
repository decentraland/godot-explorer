<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permission to allow the app to access the internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Permissions to allow the app to read and write calendar events -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <!-- Permission to post notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Permission to schedule exact alarms -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application>
        <!--
        Plugin metadata:

        - In the `android:name` attribute, the `org.godotengine.plugin.v2` prefix
        is required so Godot can recognize the project as a valid Godot
        Android plugin. The plugin name following the prefix should match the value
        of the plugin name returned by the plugin initializer.

        - The `android:value` attribute should be the classpath to the plugin
        initializer.
        -->
        <meta-data
            android:name="org.godotengine.plugin.v2.${godotPluginName}"
            android:value="${godotPluginPackageName}.GodotAndroidPlugin"/>

        <!--
        Firebase Test Lab Android Game Loop entry point.

        A dedicated EXPORTED activity-alias so Test Lab (and a local `adb shell
        am start` with the TEST_LOOP action + a scenario extra) can launch the app
        in Game Loop mode. Godot's real activity `com.godot.game.GodotApp` is
        exported=false, so we add a separate exported alias that forwards the intent
        to it. On launch getLaunchIntentData() sees the TEST_LOOP action + scenario
        extra, which the GameLoopRunner autoload reads. This is a brand-new element
        (not a merge onto Godot's own launcher), so it needs no tools:node operation.
        -->
        <activity-alias
            android:name="com.godot.game.GameLoopLauncher"
            android:targetActivity="com.godot.game.GodotApp"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.intent.action.TEST_LOOP"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/javascript"/>
            </intent-filter>
        </activity-alias>

        <!-- BroadcastReceiver for handling scheduled notifications -->
        <receiver
            android:name="${godotPluginPackageName}.NotificationReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="${godotPluginPackageName}.NOTIFICATION_ACTION"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
