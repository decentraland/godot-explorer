name: 🗑️ Sync Branch Deletion

on:
  delete:

jobs:
  delete-branch:
    name: Delete branch in deploy pipeline
    if: github.event.ref_type == 'branch'
    runs-on: warp-ubuntu-latest-x64-2x

    steps:
      - name: Setup SSH for deployment
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Delete branch in mobile deploy pipeline
        run: |
          # Determine branch name based on event type
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BRANCH_NAME="PR-${{ github.event.pull_request.number }}-${{ github.head_ref || github.ref_name }}"
          else
            BRANCH_NAME="${{ github.head_ref || github.ref_name }}"
          fi

          echo "Branch '$BRANCH_NAME' was deleted from source repository"
          echo "Attempting to delete it from deploy pipeline..."

          # Clone the target repository using SSH
          GIT_SSH_COMMAND='ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes' git clone git@github.com:decentraland/godot-mobile-deploy-pipeline.git deploy-repo

          cd deploy-repo

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Check if branch exists in deploy repo
          if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
            echo "Branch '$BRANCH_NAME' exists in deploy pipeline. Deleting..."
            GIT_SSH_COMMAND='ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes' git push origin --delete "$BRANCH_NAME"
            echo "✅ Branch '$BRANCH_NAME' deleted from deploy pipeline"
          else
            echo "ℹ️ Branch '$BRANCH_NAME' does not exist in deploy pipeline. Nothing to delete."
          fi

          # Cleanup
          cd ..
          rm -rf deploy-repo

          echo "✅ Branch deletion synchronized"
          echo "📦 Deploy Repository: https://github.com/decentraland/godot-mobile-deploy-pipeline"
          echo "🗑️ Deleted Branch: ${BRANCH_NAME}"

      - name: Cleanup SSH key
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
