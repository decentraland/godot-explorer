# HudButton — normalized icons

One glyph per button (constant across states; only the background changes):

- `chat.png`
- `emote_wheel.png`
- `chat_flip.png`
- `discover.png`

Normalized copies (PNG → VRAM Compressed high quality; SVG → DPITexture). Drop new raw art
in `../_raw/` and re-run the normalize step rather than editing the `.import` files here.
