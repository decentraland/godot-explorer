# navbar icons

Navbar-owned icon assets (SVG), one per navbar entry, in two states:

- `<name>.svg` — **selected** state (filled).
- `<name>-outlined.svg` — **default** state (outline).

This mirrors the convention used in `organisms/menu/icons`. Drop the SVGs here and
Godot will generate the paired `.svg.import` files on the next import.
